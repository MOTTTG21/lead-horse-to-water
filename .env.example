# Copy to .env and fill in real values. .env is gitignored -- never commit
# real secrets. In production these come from GCP Secret Manager instead
# (see docs/design-plan.md, "Stack").

ANTHROPIC_API_KEY=your-key-here
